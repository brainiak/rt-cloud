Bootstrap: docker
From: brainiak/rtcloud:latest

%post
    source /opt/.bashrc
    conda init
    cp -r /opt/.vnc ~/.vnc
    cp -r /rt-cloud /tmp
    echo '. /opt/.bashrc' >> /rtcloud.sh
    echo 'cd /tmp/rt-cloud' >> /rtcloud.sh